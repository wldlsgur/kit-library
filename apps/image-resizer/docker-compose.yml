services:
  image-resizer:
    build: .
    image: image-resizer:latest
    container_name: image-resizer
    ports:
      - "${PORT:-8080}:8080"
    environment:
      PORT: 8080
      DEFAULT_QUALITY: 80
      CACHE_MAX_AGE: 86400
    restart: unless-stopped
